# Pinned 2026-08-13 after first verified install on Python 3.13.2 (see ROLLBACK.md)

# Core — needed from Phase 0
numpy==2.5.2
pandas==3.0.5
PyYAML==6.0.3
matplotlib==3.11.1
pytest==9.1.1

# Phase 3+ — DQN, policy gradient, reward model (CPU build; imports verified on 3.13)
torch==2.13.0

# Phase 5+ — labelling UI and dashboard backend (optional)
fastapi==0.141.1
uvicorn==0.52.2

# Phase 6 — Cohen's kappa, statistical checks
scikit-learn==1.9.0

# NOT ALLOWED without human approval (CONSTRAINTS.md #7, #8):
#   stable-baselines3, ray[rllib], or any RL library.
#   Every syllabus algorithm is written by hand.
